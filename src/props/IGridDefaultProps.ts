import { IProps } from "../interfaces";

export interface IGridDefaultProps extends IProps {
  sm?: number;
  auto?: number;
  col?: number;
  xs?: number;
  md?: number;
  lg?: number;
  xl?: number;
}
